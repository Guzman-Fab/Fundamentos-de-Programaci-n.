#include <iostream>
#include <cmath>
using namespace std ;
int main ()
{
    double x1, x2, z1, z2 ;
    float a, b, c, determinante ;
    cout<<"Ingrese el valor a:"<<endl ;
    cin>>a ;
    cout<<"Ingrese el valor b:"<<endl ;
    cin>>b ;
    cout<<"Ingrese el valor c:"<<endl ;
    cin>>c ;
    determinante=(b*b-4*a*c) ;
    x1=(-b+sqrt(determinante))/(2*a) ;
    x2=(-b-sqrt(determinante))/(2*a) ;
    z1=(-b)/(2*a) ;
    z2=(-b)/(2*a) ;
    if (a==0)
        cout<<"No se puede realizar" ;
    else
        if (determinante>=0)
        {
            cout<<"Es del conjunto reales"<<endl ;
            cout<<"La primera raiz es:"<<x1<<endl ;
            cout<<"La segunda raiz es:"<<x2<<endl ;
        }
        else
        {
            cout<<"Es del conjunto irreal"<<endl ;
            cout<<"La primera raiz es:"<<z1<<"*+"<<sqrt(-determinante)<<"i"<<endl ;
            cout<<"La segunda raiz es:"<<z2<<"*-"<<sqrt(-determinante)<<"i"<<endl ;
        }
    return 0 ;
}
